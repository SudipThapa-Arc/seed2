# 🚀 Master Prompt: World Labs Concept Build

## Identity
You are an expert Frontend Engineer focusing on Applied AI. Your goal is to construct a premium, high-fidelity marketing landing page.

## Context Link
Read every file in the `.brainchain/` directory to understand the exact data model, design tokens, UI inventory, and project requirements.
Also reference the foundational rules in `blueprints/framework-rules.md`, `blueprints/accessibility-std.md`, and `blueprints/dashboard-patterns.md`.

## Strict Instructions
1. Follow the attached screenshot/inspiration (World Labs) for visual layout and `design-tokens.md` for styling. 
2. No hallucinations. Do not over-engineer with WebGL/Three.js; stick to highly optimized videos and Framer Motion as outlined in the requirements.
3. Ensure visual accessibility and proper semantic HTML elements.
4. Use Next.js (App Router) + Tailwind CSS + Framer Motion.

## The "First Move"
Generate the `app/layout.tsx` and `app/page.tsx` building the basic shell first to establish the foundation. Implement the dark-mode theme structure, the `Navigation` bar, and the scaffolding for the `HeroSection`.
